# Hugo build output and caches
/public/
/resources/
.hugo_build.lock

# ox-hugo and its dependencies, installed per-repo by `make export`
/.emacs-packages/

# Editor / OS
*.sublime-workspace
.DS_Store
